# WireMock Mapping Generator Environment Configuration

# Service Ports
WIREMOCK_PORT=8080
WEB_UI_PORT=5001

# WireMock Configuration (Development with CORS support)
WIREMOCK_OPTIONS=--global-response-templating --enable-browser-proxying --verbose

# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=1

# Application Settings
PYTHONPATH=/app
WIREMOCK_URL=http://wiremock:8080

# Development Options
COMPOSE_PROJECT_NAME=wiremock-mapping-generator

# Environment Presets (uncomment to override WIREMOCK_OPTIONS)
# Testing/CI
# WIREMOCK_OPTIONS=--global-response-templating --quiet --disable-banner --enable-browser-proxying

# Production
# WIREMOCK_OPTIONS=--global-response-templating --no-request-journal --enable-browser-proxying
